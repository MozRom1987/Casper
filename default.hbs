<!DOCTYPE html>
<html lang="{{@site.locale}}">

<head>

    {{!-- Basic meta --}}
    <title>{{meta_title}}</title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="HandheldFriendly" content="True" />
    <meta name="MobileOptimized" content="320" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    {{!-- Groovy CSS --}}
    <link rel="stylesheet" type="text/css" href='{{asset "css/plugins.min.css"}}' />
    <link rel="stylesheet" type="text/css" href='{{asset "css/screen.css"}}' />
    {{!-- NeiroBook overrides --}}
    <link rel="stylesheet" type="text/css" href='{{asset "css/neiro.css"}}' />

    {{!-- Google Fonts: Jost + Outfit --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Jost:ital,wght@0,400;0,500;0,600;0,700;1,400;1,700&family=Outfit:wght@400;700;800&display=swap"
        rel="stylesheet">

    {{!-- Tag & category colors --}}
    <style>
        :root {
            --epcl-background-color: #54CBCA;
            --epcl-content-background-color: #ffffff;
        }

            {
                {
                #get "tags" limit="all"
            }
        }

            {
                {
                #foreach tags
            }
        }

        .ctag- {
                {
                slug
            }
        }

            {
            --ctag-color: {
                    {
                    #if accent_color
                }
            }

                {
                    {
                    accent_color
                }
            }

                {
                    {
                    else
                }
            }

            var(--main-color) {
                    {
                    /if
                }
            }

            ;
        }

        .ctag- {
                {
                slug
            }
        }

        svg {
            fill: {
                    {
                    #if accent_color
                }
            }

                {
                    {
                    accent_color
                }
            }

                {
                    {
                    else
                }
            }

            var(--main-color) {
                    {
                    /if
                }
            }

            !important;
        }

        .ctag- {
                {
                slug
            }
        }

        .epcl-category-overlay {
            background: {
                    {
                    #if accent_color
                }
            }

                {
                    {
                    accent_color
                }
            }

                {
                    {
                    else
                }
            }

            #fff {
                    {
                    /if
                }
            }

            ;
        }

        .ctag- {
                {
                slug
            }
        }

        .epcl-category-overlay:before {
            background: {
                    {
                    #if accent_color
                }
            }

                {
                    {
                    accent_color
                }
            }

                {
                    {
                    else
                }
            }

            var(--main-color) {
                    {
                    /if
                }
            }

            ;
        }

        .main-article.primary-tag- {
                {
                slug
            }
        }

        .epcl-category-overlay {
            background: {
                    {
                    #if accent_color
                }
            }

                {
                    {
                    accent_color
                }
            }

                {
                    {
                    else
                }
            }

            #fff {
                    {
                    /if
                }
            }

            ;
        }

        .main-article.primary-tag- {
                {
                slug
            }
        }

        .epcl-category-overlay:before {
            background: {
                    {
                    #if accent_color
                }
            }

                {
                    {
                    accent_color
                }
            }

                {
                    {
                    else
                }
            }

            var(--main-color) {
                    {
                    /if
                }
            }

            ;
        }

            {
                {
                /foreach
            }
        }

            {
                {
                /get
            }
        }

        #header.is-sticky div.menu-wrapper .bg-box {
            box-shadow: none;
        }

        #author .author-avatar .author-image {
            border-radius: 100%;
        }
    </style>

    <script>
        var site_url = '{{@site.url}}';
        var theme_config = {
            masonry: 'on',
            sticky_sidebar: 'on',
            disqus_shortname: ''
        };
    </script>

    {{ghost_head}}

</head>

<body class="{{body_class}}">

    {{!-- SVG Icons sprite --}}
    {{> "svg-icons"}}

    {{!-- Mobile Navigation --}}
    <nav class="mobile main-nav hide-on-desktop-lg">
        <ul class="menu">
            {{#foreach @site.navigation}}
            <li class="nav-{{slug}}{{#if current}} current-menu-item{{/if}}"><a href="{{url}}">{{label}}</a></li>
            {{/foreach}}
            <li class="nav-login">
                {{#unless @member}}
                <a href="#/portal/signup" class="login epcl-button dark" data-title="Sign Up">Sign Up</a>
                {{else}}
                <a href="#/portal/account" class="login epcl-button dark" data-title="Account">Account</a>
                {{/unless}}
            </li>
        </ul>
    </nav>
    <div class="menu-overlay hide-on-desktop-lg"></div>

    {{!-- Main wrapper --}}
    <div id="wrapper">

        {{!-- Header --}}
        <header id="header" class="enable-sticky">

            <div class="menu-mobile">
                <svg class="icon ularge open">
                    <use xlink:href="#menu-icon"></use>
                </svg>
                <svg class="icon ularge close">
                    <use xlink:href="#close-icon"></use>
                </svg>
            </div>

            <div class="menu-wrapper">
                <div class="grid-container">
                    <div class="bg-box">
                        {{!-- Logo --}}
                        <div class="logo">
                            <a href="{{@site.url}}">
                                {{#if @site.logo}}
                                <img src="{{@site.logo}}" alt="{{@site.title}}" />
                                {{else}}
                                <span class="title">{{@site.title}}</span>
                                {{/if}}
                            </a>
                        </div>

                        {{!-- Search --}}
                        <a href="javascript:void(0)" data-ghost-search class="epcl-search-button button circle dark">
                            <svg class="icon">
                                <use xlink:href="#search-icon"></use>
                            </svg>
                        </a>

                        {{!-- Desktop nav --}}
                        <nav class="main-nav">
                            <ul class="menu">
                                {{#foreach @site.navigation}}
                                <li class="nav-{{slug}}{{#if current}} current-menu-item{{/if}}"><a
                                        href="{{url}}">{{label}}</a></li>
                                {{/foreach}}
                                <li class="nav-login">
                                    {{#unless @member}}
                                    <a href="#/portal/signup" class="login epcl-button dark">Sign Up</a>
                                    {{else}}
                                    <a href="#/portal/account" class="login epcl-button dark">Account</a>
                                    {{/unless}}
                                </li>
                            </ul>
                        </nav>

                        {{!-- Social buttons --}}
                        {{#if @site.facebook}}
                        <div class="epcl-social-buttons fill-color hide-on-tablet hide-on-mobile hide-on-desktop-sm">
                            {{#if @site.facebook}}<a href="https://www.facebook.com/{{@site.facebook}}" class="facebook"
                                target="_blank" rel="nofollow noopener"><i class="fa fa-facebook"></i></a>{{/if}}
                            {{#if @site.twitter}}<a href="https://twitter.com/{{@site.twitter}}" class="twitter"
                                target="_blank" rel="nofollow noopener"><svg>
                                    <use xlink:href="#twitter-icon"></use>
                                </svg></a>{{/if}}
                            <a href="{{@site.url}}/rss/" class="rss" target="_blank"><i class="fa fa-rss"></i></a>
                        </div>
                        {{/if}}

                        <div class="clear"></div>
                    </div>
                    <div class="clear"></div>
                </div>
            </div>
            <div class="clear"></div>
        </header>

        {{{body}}}

        {{!-- Footer --}}
        <footer id="footer">

            <div class="widgets grid-container grid-large hide-on-mobile">
                <div class="epcl-row">

                    {{!-- About widget --}}
                    <section class="widget widget_text bg-box grid-33 tablet-grid-50 mobile-grid-100">
                        <div class="logo">
                            <a href="{{@site.url}}">
                                {{#if @site.logo}}
                                <img src="{{@site.logo}}" alt="{{@site.title}}" width="170" />
                                {{else}}
                                <span class="title">{{@site.title}}</span>
                                {{/if}}
                            </a>
                        </div>
                        <div class="textwidget">
                            {{#if @site.description}}<p>{{@site.description}}</p>{{/if}}
                        </div>
                        <div class="clear"></div>
                    </section>

                    {{!-- Latest articles widget --}}
                    <div class="widget widget_epcl_posts_thumbs bg-box grid-33 tablet-grid-50 mobile-grid-100">
                        <h4 class="widget-title title medium bordered">Latest Articles</h4>
                        {{#get "posts" limit="4"}}
                        {{#foreach posts}}
                        <article class="item ctag-{{primary_tag.slug}}">
                            <div class="info">
                                {{#if feature_image}}
                                <a href="{{url}}" class="thumb main-effect epcl-loader">
                                    <span class="fullimage cover lazy"
                                        data-src='{{img_url feature_image size="s"}}'></span>
                                    <span class="epcl-number">{{@number}}</span>
                                </a>
                                {{/if}}
                                <div class="right">
                                    <h4 class="title usmall underline-effect"><a href="{{url}}">{{title}}</a></h4>
                                    <time datetime='{{date format="YYYY-MM-DD"}}'>{{date format="MMM DD, YYYY"}}</time>
                                </div>
                            </div>
                            <div class="clear"></div>
                        </article>
                        {{/foreach}}
                        {{/get}}
                    </div>

                    {{!-- Tags widget --}}
                    <div class="widget bg-box grid-33 tablet-grid-50 mobile-grid-100">
                        <h4 class="widget-title title medium bordered">Categories</h4>
                        {{#get "tags" limit="10"}}
                        <div class="widget_tag_cloud">
                            {{#foreach tags}}
                            <a href="{{url}}" class="ctag ctag-{{slug}}" data-id="ctag-{{slug}}">
                                <svg>
                                    <use xlink:href="#tag-decoration"></use>
                                </svg>{{name}}
                            </a>
                            {{/foreach}}
                        </div>
                        {{/get}}
                    </div>

                </div>
            </div>

            {{!-- Newsletter --}}
            <div class="epcl-subscribe textcenter bg-box grid-container grid-usmall">
                <h3 class="title medium">Stay in the loop!</h3>
                <h4 class="title large">Subscribe to our Newsletter</h4>
                <form class="subscribe-form" data-members-form="subscribe">
                    <div class="form-group">
                        <input type="email" name="email" class="inputbox rounded large" required
                            placeholder="Enter your email address" data-members-email />
                        <button type="submit" class="submit absolute epcl-button">
                            Subscribe <svg class="icon">
                                <use xlink:href="#send-email"></use>
                            </svg>
                        </button>
                    </div>
                    <p class="success-message"><i class="fa fa-check"></i> You're subscribed!</p>
                    <p class="error-detail"></p>
                </form>
            </div>

            {{!-- Copyright --}}
            <div class="published textcenter" style="padding: 20px 0;">
                <p>&copy; {{date format="YYYY"}} <a href="{{@site.url}}">{{@site.title}}</a>. Powered by <a
                        href="https://ghost.org" target="_blank">Ghost</a>.</p>
            </div>

            {{!-- Secondary navigation --}}
            {{#if @site.secondary_navigation}}
            <nav class="secondary-nav textcenter" style="padding-bottom: 20px;">
                <ul>
                    {{#foreach @site.secondary_navigation}}
                    <li><span class="sep"></span><a href="{{url}}">{{label}}</a></li>
                    {{/foreach}}
                </ul>
            </nav>
            {{/if}}

            {{!-- Back to top --}}
            <a id="back-to-top" href="#" title="Back to top">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
                    <path d="M12 4l-8 8h5v8h6v-8h5z" />
                </svg>
            </a>

        </footer>

    </div>

    <div class="body-decorations"></div>

    {{!-- Groovy JS --}}
    <script src='{{asset "js/groovy.js"}}'></script>

    {{ghost_foot}}

</body>

</html>